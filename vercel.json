{
  "buildCommand": "rm -rf node_modules .next && NODE_OPTIONS='' npm ci && NODE_OPTIONS='' npm run build",
  "devCommand": "npm run dev",
  "installCommand": "NODE_OPTIONS='' npm ci",
  "framework": "nextjs"
}

