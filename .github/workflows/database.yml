name: Database - Migrations

on:
  push:
    paths:
      - 'apps/web/prisma/schema.prisma'
      - 'apps/web/prisma/migrations/**'
    branches: [ main ]
  workflow_dispatch:

# Serialize production migration runs. Concurrent runs caused the 2026-06-28
# P3009 "relation already exists" incident; cancel-in-progress:false lets an
# in-flight migration finish rather than being killed mid-DDL.
concurrency:
  group: migrate-production
  cancel-in-progress: false

env:
  NODE_VERSION: '20'

# Baseline migrations list — all migrations that existed before Prisma migration tracking
# was introduced. These are marked as "already applied" via `migrate resolve` so that
# `migrate deploy` does not attempt to re-run DDL that the Supabase-native migration
# system already applied. Add new migrations below this list (do NOT add them here).
jobs:
  migrate-production:
    name: Run Migrations - Production
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://disasterrecovery.com.au
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Baseline — mark pre-existing migrations as applied
        run: |
          cd apps/web
          # The 24 pre-squash migrations were archived to prisma/migrations-archive/
          # (they only ever built 26 of 142 tables; prod's full schema came from
          # `db push`). They remain recorded in prod's _prisma_migrations from prior
          # deploys — harmless history. The squashed baseline below reproduces the
          # entire schema; resolving it --applied stops prod's migrate deploy from
          # trying to recreate the 142 already-present tables.
          BASELINES=(
            "00000000000000_init_baseline"
          )
          for migration in "${BASELINES[@]}"; do
            echo "Resolving baseline: $migration"
            pnpm exec prisma migrate resolve --applied "$migration" || true
          done
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
          DIRECT_URL: ${{ secrets.DIRECT_URL_PRODUCTION }}
      # Incident recovery (2026-06-28): the 2026-06-28 08:21 UTC deploy of
      # 20260628030000 committed its CREATE TABLE/indexes transaction but a racing
      # concurrent deploy then hit "relation already exists" (42P07), leaving the
      # migration recorded as FAILED → P3009 blocked all later migrations. Because
      # Prisma runs each migration in a single Postgres transaction, the table and
      # both indexes (incl. the contractorId+version unique guard) are fully present,
      # so marking it applied is correct and non-destructive. Idempotent (|| true);
      # safe to leave in place.
      - name: Recover failed migration 20260628030000 (P3009)
        run: |
          cd apps/web
          pnpm exec prisma migrate resolve --applied "20260628030000_add_contractor_agreement_acceptance" || true
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
          DIRECT_URL: ${{ secrets.DIRECT_URL_PRODUCTION }}
      - name: Run Prisma migration
        run: pnpm --filter nrpg-web exec prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
          DIRECT_URL: ${{ secrets.DIRECT_URL_PRODUCTION }}
      - name: Generate Prisma client
        run: pnpm --filter nrpg-web exec prisma generate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
      - name: Run post-migration checks
        run: pnpm --filter nrpg-web exec prisma db seed --skip-generate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL_PRODUCTION }}
        continue-on-error: true
      - name: Notify on success
        if: success()
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d '{"text":"Database migrations completed successfully"}'
        continue-on-error: true
      - name: Notify on failure
        if: failure()
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d '{"text":"Database migrations FAILED"}'
        continue-on-error: true
