/**
 * Embedded training module data
 * This ensures modules are always available even if static files fail to deploy
 */

export const EMBEDDED_MODULES: Record<string, any> = {};

// This file will be populated with actual module data
// For now, it's a placeholder to enable the fallback mechanism
