
# Workaround for Vercel sandbox NODE_OPTIONS incompatibility
# The --max-old-space-size flag is not supported in Vercel's sandboxed environment
# npm_config_node_options is ignored to prevent conflicts
ignore-scripts=false
legacy-peer-deps=true
