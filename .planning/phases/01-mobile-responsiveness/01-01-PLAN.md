---
phase: 01-mobile-responsiveness
plan: 01
type: execute
domain: react-mobile
---

<objective>
Optimize animations and interactions for mobile devices (iOS/Android).

Purpose: Ensure premium animations and form interactions perform smoothly on mobile without jank, respect touch interactions, and maintain usability on small screens.

Output: Mobile-optimized codebase with tested animations on iOS and Android, improved form inputs for touch, and verified responsive modals.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@CLAUDE.md (Project constraints and architecture)

**Tech Stack Available:**
- Framer Motion (40+ animation variants, context-aware)
- React Hook Form (form handling with validation)
- Tailwind CSS (responsive utilities: sm:/md:/lg:/xl:/2xl:)
- Next.js 14 (App Router, client/server components)

**Key Files to Reference:**
- lib/animations.ts - All animation definitions
- lib/hooks/useAnimation.ts - Animation control hook
- src/design-system/components/Form/FormInput.tsx - Enhanced form input
- src/design-system/components/Layout/PageTransition.tsx - Page transitions
- app/page.tsx - Home page (main mobile entry point)
- app/claim/step-1|2|3/page.tsx - Claim wizard (critical mobile UX)

**Mobile Touch Points:**
- Home page header/navigation (hamburger menu)
- Emergency CTA buttons (tap targets)
- Claim form fields (input focus, validation messages)
- Modal dialogs (keyboard/overlay behavior)
- Card hover effects (should be tap-safe on mobile)

**Issues to Address:**
- Animation performance on low-end mobile devices
- Touch interaction feedback (buttons, form fields)
- Form input sizing for mobile keyboards
- Modal viewport and scroll behavior on iOS
- Prefers-reduced-motion accessibility on mobile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Framer Motion animations for mobile performance</name>
  <files>lib/animations.ts, lib/hooks/useAnimation.ts</files>
  <action>
    1. Add mobile-aware animation variants that reduce duration/complexity on small screens
    2. Create isMobileDevice() utility to detect mobile at runtime
    3. Add animation quality presets: reduced (100ms), mobile (200ms), desktop (400ms)
    4. Modify pageTransitions to use 200ms on mobile instead of 400ms
    5. Add reduceMotion parameter to useAnimation hook to respect device motion preferences
    6. For scroll reveals: disable on mobile (replace with fade-in only)
    7. For card hovers: convert to tap feedback on touch devices (no hover state)

    What to avoid and WHY:
    - Don't use expensive animations (blur, shadow filters) on mobile - GPU-limited devices will jank
    - Don't animate on scroll on mobile (battery + performance)
    - Don't use transform: translateZ or will-change on non-critical elements (mobile GPU thrashing)

    Implementation approach:
    - Add device detection via user-agent and media query checking
    - Create animation quality tier system based on device
    - Use React.useCallback to memoize animation selections
  </action>
  <verify>
    - npm run build succeeds (no TypeScript errors)
    - Animation variants export properly from lib/animations.ts
    - useAnimation hook accepts reduceMotion parameter
    - Animation durations are 200ms on mobile, 400ms on desktop
  </verify>
  <done>
    Mobile animations defined and exported, hook updated, no build errors, ready for component integration
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix touch interactions and form input usability on mobile</name>
  <files>src/design-system/components/Form/FormInput.tsx, src/design-system/components/Button/Button.tsx, src/design-system/components/Modal/Modal.tsx</files>
  <action>
    1. **FormInput.tsx:**
       - Ensure minimum 44px height on mobile (touch target size)
       - Add iOS-safe input styling (disable input zoom on focus)
       - Add -webkit-appearance: none to remove browser default styling
       - Increase padding on mobile (sm: more padding for fat-finger friendliness)
       - Ensure error messages don't shift layout (fixed height container)
       - Add font-size: 16px minimum (prevents iOS auto-zoom on focus)

    2. **Button.tsx:**
       - Ensure minimum 48x48px tap targets on mobile
       - Add :active state for mobile feedback (visual change on press)
       - Remove hover states on touch devices (use @media (hover: none))
       - Add smooth background-color transition (200ms) for tap feedback

    3. **Modal.tsx:**
       - Fix overflow on iOS (position: fixed doesn't work well)
       - Add padding-bottom: safe-area-inset-bottom for notched devices
       - Ensure modal content scrolls internally (not page scroll behind)
       - Add position: relative to overlay to prevent scroll jank
       - Test: Modal should close on mobile with swipe-down gesture (if already implemented)

    What to avoid and WHY:
    - Don't use outline on inputs (can't see on some mobile browsers with dark mode)
    - Don't use ::before/::after pseudo-elements for tap targets (not keyboard accessible)
    - Don't use touch-action: manipulation on modal (breaks iOS gestures)
    - Don't use -webkit-touch-callout: none unless you really need to disable long-press menu
  </action>
  <verify>
    - npm run build succeeds
    - FormInput renders with 44px+ height on mobile
    - Button tap targets are 48x48px or larger
    - Modal doesn't have scrolling jank (no double-scroll)
    - No console errors on mobile browsers
  </verify>
  <done>
    FormInput, Button, Modal all optimized for touch. Tap targets correct. Scrolling smooth. No console errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Mobile-optimized Framer Motion animations (reduced duration, no scroll reveals)
    - Touch-friendly form inputs and buttons (44-48px tap targets)
    - Fixed modal behavior on iOS/Android
  </what-built>
  <how-to-verify>
    **On iOS (iPhone or Safari simulator):**
    1. Open http://localhost:3000 (home page)
    2. Verify header/hero page transition is smooth (no jank) - should complete in ~200ms
    3. Tap "Request Service" button - should feel responsive (visual feedback on press)
    4. Scroll down - no choppy animation artifacts, cards appear quickly
    5. Tap claim form link → Step 1 page should load smoothly
    6. Test form inputs:
       - Tap input field (should show 44px+ clickable area)
       - Type text (should NOT zoom page to 100% when focused)
       - Fill all fields, verify error messages don't cause layout shift
    7. Test responsive: Rotate device to landscape - layout should adjust smoothly

    **On Android (Chrome DevTools Mobile or real device):**
    1. Open http://localhost:3000
    2. Repeat same tests as iOS
    3. Verify tap feedback on buttons (color change visible)
    4. Test claim form again (Android keyboard behavior different from iOS)
    5. Open modal/dialog - verify no double-scrolling

    **Chrome DevTools Desktop (simulate mobile):**
    1. Open DevTools → Device Toolbar (Ctrl+Shift+M)
    2. Switch to iPhone 12/Pro/Max and Pixel 5
    3. Verify animations run smoothly (60fps) - check Performance tab if needed
    4. Verify form inputs accessible and buttons tappable

    **Expected behaviors:**
    - Animations complete smoothly within 200ms on mobile
    - No layout shifts when errors appear
    - Buttons provide immediate visual feedback on tap
    - Forms don't zoom page when focused
    - Modals don't have scroll jank
    - No console warnings about touch handling
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass on iOS and Android, or describe specific issues you found</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Mobile animations optimized and perform smoothly (200ms duration on mobile)
- [ ] Form inputs have 44px+ touch targets with proper focus states
- [ ] Buttons provide tap feedback and are 48x48px or larger
- [ ] Modals scroll internally without page jank
- [ ] No console errors or warnings on iOS/Android browsers
- [ ] Tested on at least 2 different mobile devices/browsers (iOS Safari + Chrome Android)
- [ ] PageTransition, FormInput, Button, Modal all modified and working
</verification>

<success_criteria>

- All tasks completed and verified
- Mobile animations no longer cause jank on real devices
- Form inputs are accessible and user-friendly on mobile
- Modal behavior is smooth on iOS and Android
- Verified on both iOS and Android platforms
- No TypeScript errors or build warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-responsiveness/01-01-SUMMARY.md`:

# Phase 01-Mobile: Plan 01 Summary

**Mobile animations and touch interactions optimized for iOS/Android**

## Accomplishments

- Added mobile-aware animation quality presets (reduced/mobile/desktop durations)
- Implemented device detection for animation complexity reduction
- Fixed form input touch targets (44px+ on mobile)
- Updated button tap feedback for mobile
- Fixed modal scrolling behavior on iOS/Android
- Verified smooth performance on real mobile devices

## Files Created/Modified

- `lib/animations.ts` - Added mobile animation variants and quality presets
- `lib/hooks/useAnimation.ts` - Added device detection and mobile mode parameter
- `src/design-system/components/Form/FormInput.tsx` - Touch target sizing, focus fix
- `src/design-system/components/Button/Button.tsx` - Tap feedback, active states
- `src/design-system/components/Modal/Modal.tsx` - Fixed overflow and scrolling

## Decisions Made

- Mobile animations use 200ms duration (vs 400ms desktop) for perceived speed
- Scroll reveals disabled on mobile (battery + performance trade-off)
- Card hovers convert to tap feedback on touch devices
- Form inputs use 44px minimum height (iOS human interface guidelines)
- Buttons use 48x48px minimum (Android material design)

## Issues Encountered

[Describe any issues found during testing and how they were resolved]

## Next Step

Ready for next phase: Public claim form completion flow or mobile analytics/testing suite
</output>

